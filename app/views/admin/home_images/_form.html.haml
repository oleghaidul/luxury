= semantic_form_for [:admin, @home_image], :html => {:multipart => true} do |f|
  = f.inputs "Basic" do
    =f.input :name
    =f.input :alt_name
    =f.input :image
    =f.input :timeout
    =f.buttons